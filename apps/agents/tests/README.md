# Tests

Layout matches project structure: `unit/`, `integration/`, `sample_pos/`. Add pytest modules when you are ready to expand coverage.

Run (from repo root, with dev deps installed):

```bash
pytest tests/
```
